pipeline {

          agent {
		  
		      label {
			  
			      label "built-in"
				  customWorkspace "/mnt/project/"
			  }
		  }
		  
		  stages {
		  
		     stage ("deploy game-of-") {
			 
			     steps {
				 //sh "docker stop server"
				 //sh "docker rm server"
				 //sh "rm -rf /mnt/project/dockerfile"
				 sh "mvn install"
				 sh "cp /mnt/project/gameoflife-web/target/gameoflife.war /mnt/project/yaml"
				 sh "docker-compose up -d"
				 			
				 }
			 
			 
			 }
		  
		  
		  }
		  
}
